수업 목표:
- 로컬에서 Git 프로젝트를 만들 수 있다
- GitHub에 원격 저장소를 만들고 연결할 수 있다
- add → commit → push 흐름을 이해하고 직접 할 수 있다
- 브랜치를 나눠 기능을 개발할 수 있다
- Pull Request로 코드 리뷰 & 병합을 할 수 있다
- 충돌(conflict)이 나도 겁먹지 않고 해결할 수 있다

>Git 기본 흐름(add·commit·push) 실습, GitHub 원격 저장소 연결 및 개인 리포지토리 생성으로 협업 기반 다지기

### Git 기본 개념

Git은 무엇인가?
	Git은 파일을 저장하는 도구가 아니라 “파일의 변경 이력”을 관리하는 도구입니다.

✔ Git으로 할 수 있는 것
- 언제, 누가, 무엇을 바꿨는지 기록
- 실수해도 과거 버전으로 되돌리기
- 여러 명이 동시에 작업해도 코드가 섞이지 않게 관리
    
###### ✔ 2. Git과 GitHub 차이
| 구분  | Git                | GitHub            |
| --- | ------------------ | ----------------- |
| 역할  | 버전 관리 도구           | Git 저장소를 보관하는 사이트 |
| 위치  | 내 컴퓨터 (WSL)        | 인터넷(클라우드)         |
| 비유  | 워드의 “되돌리기 기록”      | 구글 드라이브           |
| 핵심  | `git add / commit` | PR, 리뷰, 협업        |
GitHub는 Git 없이는 의미가 없고 Git은 GitHub 없이도 쓸 수 있다

---
Git 설치 확인 (WSL)
```bash
git --version
```

없다면 설치
```bash
sudo apt update
sudo apt install git -y
```

---

### Git 초기 설정 + 첫 commit

✔ Git 사용자 정보 설정 (필수)
```bash
git config --global user.name "홍길동" 
git config --global user.email "youremail@example.com"
```
이 정보가 **commit 작성자**에 기록됩니다! 커밋 작성자 정보가 없으면 협업이 안 됨

이 정보는:
- GitHub 계정과 이메일이 같아야
- 커밋이 내 GitHub 계정과 연결됨
![[Pasted image 20251218163040.png]]
GitHub 프로필 화면 기준
- GitHub 사용자명(username): `handgonpo`
- GitHub 표시 이름(Name): `Lee Youjung`
- 브랜치 이름: `main`
- GitHub 계정 이메일: handgonpo@naver.com

Git에서 쓰이는 `user.name`, `user.email` 확인 방법
```bash
git config --global --list
```

GitHub 계정에 등록된 이메일 확인 방법
① GitHub → Settings
② 왼쪽 메뉴 → Emails
```
Settings → Emails
```
---
### 로컬에서 Git 프로젝트 만들기 (폴더 생성부터)

🎯 실습 목표  
“아무 것도 없는 상태 → Git이 관리하는 프로젝트” 만들기

✔ 프로젝트 폴더 생성
```bash
cd ~
mkdir git_day8
cd git_day8
```

✔Git 초기화
```bash
git init
```
	이 순간: .git 폴더 생성 이 폴더는 이제 Git이 관리함

확인
```bash
ls -a
```

✔ 첫 파일 만들기
```bash
echo "print('Hello Git')" > app.py
```

상태 확인:
```bash
git status
```
	아직 Git은 이 파일을 모른다

---
### add → commit 흐름
Git 내부 흐름 시각화
```
Working Directory
   ↓ git add
Staging Area
   ↓ git commit
Repository
```
	이건 파일이 Git에 저장되는 과정을 단계별로 나타낸 그림입니다.

1️⃣ Working Directory (작업 공간)

🔹 한 줄 정의
	내가 실제로 파일을 수정하는 곳

🔹 지금 이 단계에서의 상태
- VSCode에서 코드 작성
- 파일 생성 / 수정 / 삭제
- 아직 Git은 이 변경을 저장하지 않음

예시:
```bash
print("Hello Git")
```
이걸 `app.py`에 적었다면 이 상태는 Working Directory

확인 명령어:
```bash
git status
```

출력 예:
```
modified: app.py
```

의미:
> “파일이 바뀌었는데, 아직 저장(commit)은 안 됐어”

2️⃣ Staging Area (준비 공간)

🔹 한 줄 정의
	이번에 저장(commit)할 파일을 미리 고르는 곳

🔹 왜 이 단계가 필요할까?
	한 번에 모든 파일을 저장하지 않아도 되게 하기 위해서

🔹 git add의 역할
```bash
git add app.py
```

의미:
> “이 파일은 이번 커밋에 포함시킬게요”

예시 상황:

- `app.py` 수정
- `test.py` 임시 파일 생성

이럴 때
```bash
git add app.py
```
	test.py는 커밋에 포함 안 됨 ❌

확인:
```bash
git status
```

출력 예:
```
Changesto be committed:
  app.py
```

3️⃣ Repository (저장소)

🔹 한 줄 정의
> 변경 이력이 영구적으로 저장되는 곳

🔹 git commit의 역할
```bash
git commit -m"Add login logic"
```

의미:
> “지금 Stage에 있는 파일들을 하나의 버전으로 기록해라”

이 순간:
- 시간
- 작성자
- 변경 내용이 모두 저장됨
    
확인:
```bash
gitlog
```
---
📦 비유로 이해하면

###### 📝 문서 작성 비유
| Git 단계            | 비유           |
| ----------------- | ------------ |
| Working Directory | 워드에서 문서 수정 중 |
| Staging Area      | 인쇄할 페이지 체크   |
| Repository        | PDF로 저장      |
	add = 체크, commit = 저장

---
실제 흐름 예제
```bash
# 1. 파일 수정 - 리눅스 기본 텍스트 편집기(nano)
nano app.py

# 2. 상태 확인
git status

# 3. 스테이징
git add app.py

# 4. 커밋
git commit -m"Fix greeting message"
```

이 흐름이 바로 👇
```
Working Directory
   ↓ gitadd
Staging Area
   ↓ gitcommit
Repository
```

❗ 착각하는 포인트

❌ commit = GitHub에 올리는 것이 아님
- `commit` = 내 컴퓨터에 저장
- `push` = GitHub에 올림

✅ 한 줄로 정리
> Working Directory에서 작업하고
> Staging Area에 올릴 걸 고른 다음
> Repository에 기록한다

한 문장 설명
> “Git은 바로 저장하지 않고,
> ‘이번에 저장할 파일을 고르는 단계(add)’를
> 일부러 한 번 더 둔 구조입니다.”

---
### GitHub 원격 저장소 만들기 + 연결

왜 원격 저장소가 필요한가?
- 내 컴퓨터 고장 → 코드 날아감 ❌
- 팀원이 내 코드 못 봄 ❌
- 협업 불가 ❌

✔ GitHub 리포지토리 생성
- GitHub → New repository
- 이름: `git-day8-practice`
- Public / Private 선택
- ❌ README / .gitignore 체크하지 않기
- Create repository

![[Pasted image 20251218165202.png]]

---
✔ 로컬 ↔ GitHub 연결

원격 주소 복사 후 WSL에서:
```bash
git remote add origin https://github.com/<username>/git-day8-practice.git
```

연결 확인:
```bash
git remote -v
```

기본 브랜치 이름 설정 확인
```bash
git branch -M main
```
일부 환경에서는 기본 브랜치가 `master` 일 수 있습니다 → 명확하게 main으로 변경

---
✔ 첫 push
```bash
git push -u origin main
```
	GitHub에서 파일이 올라간 것 확인

---
### README.md 만들기 실습

왜 README가 중요한가?
- 프로젝트 설명서
- 팀원/면접관이 제일 먼저 보는 파일

README 생성
```bash
echo "# Git Day8 Practice" > README.md
```

```bash
git add README.md
git commit -m "Add README"
git push
```
	GitHub에서 README 렌더링 확인

---
### 팀 협업 핵심 명령어: clone / pull / fetch

수업준비사항:
- 강사 GitHub에 실습용 저장소 1개 있음 (`git-day7-practice`)
- 학생들은 GitHub 계정 있음
- WSL에서 Git 설치 완료

https://github.com/eunice-bootcamp-team2/git-day7-practice

---
# clone 실습 = “처음 참여한 팀원” 역할

🎯 목표
- 원격 저장소를 내 컴퓨터로 “통째로” 내려받고
- 자동으로 Git 연결(remote)까지 된 상태를 확인한다

지금 우리가 하는 `git clone`은 “팀 저장소에 처음 합류한 팀원이  
코드를 내려받고 협업 준비를 마치는 단계”입니다.
아직 코드를 올리는(push) 단계는 아니고, 코드를 보고, 최신 상태를 유지하는 역할입니다.

(학생) 원하는 위치로 이동
```bash
cd ~  # 현재 로그인한 WSL 사용자(youjung)의 홈 디렉토리로 이동
mkdir git-class
cd git-class
```

(학생) clone 실행
```bash
git clone https://github.com/eunice-bootcamp-team2/git-day7-practice
```

(학생) 폴더로 이동
```bash
cd git-day7-practice
```

실제 구조
```
/home/youjung
└── git-class
    └── git-day7-practice
        ├── README.md
        └── .git/
```

“clone 하면 뭐가 자동으로 돼?” 확인 실습
```bash
ls
```

Git 저장소인지 확인
```bash
git status
```
출력:
```
On branch main  # 현재 내가 작업 중인 브랜치는 main이다
Your branch is up to date with 'origin/main'.  # 내 로컬 main 브랜치가 GitHub의 main 브랜치(origin/main)와 완전히 동일하다. 즉,받아올 최신 변경 사항도 없고, 충돌도 없는 상태
```
	이 메시지는 지금 Git 상태가 정상이고 안전하다는 뜻입니다.


remote가 자동 등록됐는지 확인
```bash
git remote -v
```

예상 출력(예시):
```bash
origin https://github.com/eunice-bootcamp-team2/git-day7-practice (fetch)
origin https://github.com/eunice-bootcamp-team2/git-day7-practice (push)
```
- clone은 단순 다운로드가 아니라, git init + remote add까지 끝난 상태로 내려온다.
- 그래서 clone만 하면 협업 준비가 된다.

---
### pull 실습 = “원격에서 바뀐 내용 최신으로 받기”

🎯 목표
- GitHub(원격)에서 파일이 바뀐 상황을 만든다
- 학생 로컬에서 `git pull`로 최신화한다

(강사) GitHub 웹에서 파일 수정
1. GitHub에서 `README.md` 클릭
2. ✏️ Edit 버튼 클릭
3. 아래 한 줄 추가
```md
## 공지
- 오늘은 pull 실습을 합니다.
```

- Commit changes 클릭 (main에 반영)

(학생) 로컬에서 pull 하기

① 현재 위치 확인
```bash
pwd
```

② pull 실행
```bash
git pull
```

예상 출력(예시):
```
Updating 1a2b3c4..5d6e7f8
Fast-forward
 README.md | 2 ++
 1 file changed, 2 insertions(+)
```

③ 변경 확인
```bash
cat README.md
```
- pull은 원격 변경을 내려받고(다운로드)
- 내 작업 폴더에 바로 반영(merge)까지 해준다
- 그래서 초보자에게는 pull이 제일 실전이다

---
### fetch 실습 = “원격 변경을 ‘미리보기’로 가져오기”

🎯 목표
- GitHub(원격)에 변화가 생긴 상황을 만든다
- 학생은 `git fetch`로 변경 내용을 “가져오기만” 한다
- 내 파일(Working Directory)은 그대로 두고,
- 원격에 뭐가 바뀌었는지만 확인한다
- 마지막에 원하면 `merge` 또는 `pull`로 적용한다

시작 전 확인 (학생이 꼭 먼저 체크)
> ⚠️ 반드시 프로젝트 폴더 안에서 진행해야 합니다.
```bash
pwd
ls
```

그리고 현재 브랜치 확인:
```bash
git branch
```

예상:
```
* main
```

원격 연결 확인:
```bash
git remote -v
```

예상:
```bash
origin  https://github.com/<username>/git-day8-practice.git (fetch) 
origin  https://github.com/<username>/git-day8-practice.git (push)
```

(강사) GitHub에서 또 수정 (원격 변화 만들기)
- `README.md` 클릭
- ✏️ Edit 버튼 클릭
- 아래 한 줄 추가
```md
- fetch는 미리보기입니다.
```
- Commit changes 클릭 (main에 반영)
이제 상황은 이렇게 됩니다:
- GitHub(원격 origin/main)에는 새 커밋이 생김
- 학생 로컬(main)은 아직 예전 상태

(학생) fetch 실행
```bash
git fetch
```
✅ 여기서 핵심 설명
- `fetch`는 “원격의 새 커밋 정보를 로컬로 다운로드만” 합니다.
- 내 파일을 자동으로 수정하지 않습니다.
- 즉, `README.md` 내용이 바로 바뀌지 않는 게 정상입니다.
    
예상 출력(환경마다 다를 수 있음):
- 아무 출력이 없을 수도 있고
- 다음처럼 나올 수도 있음
```bash
remote: Enumerating objects: 5, done.
remote: Counting objects: 100% (5/5), done.
remote: Compressing objects: 100% (3/3), done.
remote: Total 3 (delta 1), reused 0 (delta 0), pack-reused 0 (from 0)
Unpacking objects: 100% (3/3), 994 bytes | 994.00 KiB/s, done.
From https://github.com/eunice-bootcamp-team2/git-day7-practice
   f9756ed..f07c6f3  main       -> origin/main
```
	GitHub(원격 저장소)에 새로운 커밋이 생겼고, 그 변경 이력을 내 컴퓨터로 정상적으로 받아왔다
	는 뜻입니다.


(학생) “내 로컬 파일이 아직 안 바뀐 것” 확인
1) README를 직접 열어보기
```bash
cat README.md
```
✅ 기대 결과:
- 방금 GitHub에서 추가한  
    `- fetch는 미리보기입니다.`  
    문장이 아직 안 보일 수 있음(정상)
    
📌 왜 정상인가?
- fetch는 파일 반영(merge)을 안 했기 때문입니다.

(학생) 원격과 내 브랜치 차이 확인 (핵심)
가장 쉬운 확인: git status:
```bash
git status
```
상황에 따라 이런 메시지가 보일 수 있어요(예시):
- 로컬이 원격보다 뒤처졌다 류의 메시지
- 또는 단순히 working tree clean만 보일 수도 있음  
    (환경/설정에 따라 표현이 다릅니다)
    
👉 그래서 확실한 비교 명령을 같이 씁니다.

다른 방법: 원격(main)과 로컬(main) 커밋 차이 보기 (추천)
```bash
git log --oneline --decorate --all --graph --max-count=10
```
✅ 여기서 보는 포인트(학생에게 체크시키기):
- `origin/main` 라벨이 main보다 위에(최신에) 있으면  
    → 원격이 앞서 있다(새 커밋이 있다)

예시 모양(개념):
```
* f07c6f3 (origin/main, origin/HEAD) Update README.md
* f9756ed (HEAD -> main) Update README.md
* 507f19f Add README
* b32de90 first commit
* a5b1c31 first commit
```
	origin/main이 HEAD -> main보다 위에 보이면 GitHub에 새 커밋이 있고, 나는 아직 
	안 받았다는 뜻이다.

“정확히 무엇이 바뀌었는지” diff로 보기 (강력 추천)
```bash
git diff main..origin/main
```
✅ 기대 결과:
- README에 추가된 줄이 diff로 보입니다.
- 출력이 나온다는 건 곧:
    - 원격(origin/main)에 변경이 존재
    - 로컬(main)에는 아직 반영 안 됨
        
이 명령은 “파일 내용이 뭐가 다른지”를 보여주는 진짜 핵심 도구입니다.

(학생) 이제 적용하고 싶으면? (merge or pull)
방법 A) 초보자 추천: pull로 한 번에 적용 ✅
```bash
git pull
```
- `pull`은 내부적으로 fetch + merge를 한 번에 합니다.
- 그래서 “그냥 최신 코드 적용” 목적이면 pull이 편합니다.
적용 후 확인:
```bash
cat README.md
```

이제는:
```md
- fetch는 미리보기입니다.
```
가 보여야 정상입니다.

방법 B) 개념 학습용: fetch 후 merge 직접 하기
이미 fetch는 했으니 이제 merge만:
```bash
git merge origin/main
```

그 후:
```bash
cat README.md
```
✅ 이렇게 하면 학생이 아, fetch는 가져오기만 / merge가 적용이구나를 확실히 체감합니다.

정리하면 (상황별 선택):
- 그냥 최신 코드 받고 싶다 → ✅ `git pull`
- 원격에 뭐 바뀌었는지 먼저 보고 싶다 → ✅ `git fetch` + 확인 후 merge/pull

---
### 브랜치 전략

왜 브랜치를 나누는가?
> ❌ `main`에서 바로 작업하면  
> → 코드가 깨질 수 있고  
> → 팀 전체가 멈출 수 있다

> ⭕ 기능별 브랜치로 작업하면  
> → 안전하게 개발하고  
> → PR로 리뷰 후 병합할 수 있다

기본 구조 (수업 표준)
```
main        ← 배포/발표(항상 동작해야 함)
dev         ← 통합 개발(여러 기능을 합치는 브랜치)
feature/*   ← 기능 단위 작업(개인이 작업하는 브랜치)
```
역할을 한 줄로 기억하기
- `main`: 완성본
- `dev`: 합치는 곳
- `feature/*`: 내 작업실

예시: 만약 본인의 브런치가 eunice인 경우
```
feature/eunice
```

또는 의미에 맞는 구체적인 브런치명
```
feature/eunice-login
feature/eunice-dashboard
feature/eunice-auth
```

왜 이름에 사람 + 기능을 같이 넣을까?
누가 작업 중인지 바로 알수 있음
```
feature/eunice-login
feature/minsu-dashboard
feature/jihye-ingest
```
GitHub 브랜치 목록에서 보면 한눈에 보입니다.

---
실제 실습: Eunice의 feature 브랜치 만들기
> ⚠️ 기준 브랜치는 dev라고 가정합니다.

① 현재 브랜치 확인
```bash
git branch
```

예상:
```
main
* dev
```

② feature 브랜치 생성 + 이동
```bash
git checkout -b feature/eunice-login
```

확인:
```bash
git branch
```

예상:
```
  main
  dev
* feature/eunice-login
```

---
0️⃣ 실습 전 준비 체크 (이걸 안 하면 100% 사고 납니다)

✅ (1) 반드시 프로젝트 폴더 안인지 확인
```bash
pwd
ls
```

✅ (2) 현재 브랜치 확인
```bash
git branch
```

예상:
```
*main
```

✅ (3) 작업 중인 변경사항(미커밋) 없는지 확인
```bash
git status
```

정상(안전) 상태:
```
nothingtocommit, working tree clean
```

⚠️ 만약 이런 게 보이면?
```
modified: ...
```

👉 브랜치 바꾸기 전에 아래 중 하나를 먼저 해야 안전합니다.
- (추천) 지금 수정한 것부터 커밋하고 이동
- (또는) 임시 저장: `git stash`

> 초보자 수업에서는 “브랜치 이동 전엔 status clean”을 규칙으로 잡는 게 좋아요.

---

### 1️⃣ dev 브랜치 만들기 (통합 브랜치) : 브런치 전략세우기 - PM의 역할

✅ 이 단계의 의미
	이 단계는 개인이 기능 개발하는 단계”가 아니라, 팀이 앞으로 협업할 기준선(dev)을 만드는 세팅 작업 입니다. 그래서 원칙적으로는 팀에서 1명(리더/PM)이 먼저 만들고 원격에 올린 뒤그 다음부터 팀원들이 dev 기준으로 feature를 뽑습니다.

🎯 목표
- main을 건드리지 않고
- dev에서 개발 흐름을 시작한다

상태 점검(중요)
- `main` 존재
- `feature/eunice-login` 존재(방금 만든 상태)
- 아직 `dev` 없음
    
이 상황에서 dev를 만들려면 반드시 main으로 돌아가서 dev를 뽑아야 합니다.

> ⚠️ `feature`에서 dev를 뽑으면 기준선이 꼬일 수 있어요.  
> dev는 팀 공용 기준선이라 main 기반이어야 합니다.

---
`1-0)` 먼저: feature 브랜치에 작업/커밋이 있는지 확인
feature 브랜치에서 뭔가 작업했으면 dev 만들기 전에 확인해야 해요.
```bash
git status
```
- working tree clean이면 ✅ 그대로 진행
- `modified:` 가 있으면 →  커밋하거나 stash 하고 dev 세팅을 해야 안전합니다.
    이유: 작업 중인 변경을 들고 main/dev로 이동하면 실수로 섞여 들어가는 사고가 자주 남.

`1-1)` main으로 이동 → 원격 최신화 → dev 생성(핵심 3단계)
```bash
git checkout main         # 1) main으로 돌아가기
git pull origin main      # 2) main을 원격 최신으로 맞추기 (팀 기준선 정렬)
git checkout -b dev       # 3) dev 브랜치 생성 + dev로 이동
```
✅ `git pull origin main`이 중요한 이유
	dev는 앞으로 팀이 계속 합치는 곳이에요. 그래서 dev는 반드시
> 최신 main을 기준점으로 뽑아야 팀 기준선이 최신 상태로 고정됩니다.
> (안 그러면 dev가 옛날 main 기준으로 만들어져서 나중에 PR/merge 때 꼬일 확률이 커져요.)

---

`1-2)` dev 브랜치를 원격에도 올리기(팀 협업 준비 완료)
```bash
git push -u origin dev
```

✅ 여기까지 하면 GitHub에 `dev`가 생기고, 이제부터 팀원들은 dev를 기준으로 feature를 뽑을 수 있습니다.

---
`1-3)` 브랜치 목록 확인(정상 결과)
```bash
git branch
```

예상:
```
* dev
  feature/eunice-login
  main
```

✅ 별`(*)`이 dev에 찍혀 있으면 “dev로 이동 완료” 상태입니다.

---
### 브랜치 전략 수립 시 고려사항 (PM/리더 역할)

`1)`기준선(Base Branch)은 하나로 고정한다
	브랜치 역할을 명확히 나눕니다.
- **`main`**
    - 발표 / 배포 / 최종 결과물
    - 항상 동작해야 함
    - 직접 작업 ❌, PR로만 반영
- **`dev`**
    - 팀 공용 통합 브랜치
    - 모든 기능(feature)이 **여기로 모임**
    - 실제 개발 기준선(Base)
- **`feature/*`**
    - 개인 작업 브랜치
    - 기능 단위, 사람+기능 단위로 분기

✅ 팀의 실질적인 기준선은 `dev` 하나로 통일
→ 그래야 “지금 우리가 기준으로 삼는 코드”가 명확해지고 혼선이 없음

---
`2)` dev는 반드시 최신 main을 기준으로 만든다
	`dev`는 팀 전체의 공용 기준선이기 때문에 언제 만들어졌는지가 매우 중요합니다.
- dev는 반드시 최신 `main`을 기준으로 한 번만 생성
- 오래된 main 기준으로 dev를 만들면:
    - 이후 PR / merge 시 히스토리 꼬임
    - 불필요한 충돌 증가
👉 그래서 dev 생성은 개인 작업이 아니라 세팅 작업입니다.

---
`3)` dev 생성은 한 사람(PM/리더)이 담당한다
- dev 브랜치는 팀 전체가 공유하는 구조
- 여러 명이 동시에 만들 필요 ❌
- PM 또는 리더 1명이 생성 → 원격에 push
- 이후 팀원들은 이미 존재하는 dev를 기준으로만 작업

✅ 역할 구분
- PM: 브랜치 구조/규칙을 만든다
- 팀원: 정해진 규칙을 따라서 개발한다

---
`4)` feature 브랜치는 반드시 dev에서만 분기한다
	전략 차원의 원칙만 명시합니다.
- ❌ main → feature (금지)
- ⭕ dev → feature (원칙)

이유:
- dev에는 이미 다른 기능들이 통합되어 있음
- 같은 기준선에서 출발해야 PR 시 충돌이 최소화됨
👉 feature 브랜치는 항상 dev의 현재 상태를 기반으로 작업

---
`5)` PR 흐름을 고정해 혼선을 막는다
	브랜치 간 이동 경로를 단순화합니다.
- **feature → dev**
    - 기능 단위 PR
    - 개발 과정에서 반복적으로 발생
- **dev → main**
    - 릴리즈/발표용 PR
    - 프로젝트 마무리 단계에서만 발생

✅ 이 규칙만 지켜도 어디로 PR을 보내야 하지?”라는 질문이 사라짐

---
`6)` 전략 차원에서 강조할 팀 공통 습관 (명령어 X, 원칙 O)
- 브랜치를 바꾸기 전에
    - 작업 중인 변경사항이 있는지 항상 확인
- 내가 지금 어디(main/dev/feature)에 있는지 항상 인지

👉 이건 실습 명령어가 아니라 협업 사고를 줄이기 위한 팀 규칙입니다.

---
한 줄 요약:
> 이 파트는 코드를 치는 단계가 아니라, 팀이 Git을 어떻게 사용할지 약속을 정하는 단계입니다.
> 이 약속이 있어야 뒤의 실습이 사고 없이 굴러갑니다.

---

### 2️⃣ feature 브랜치 만들기 (개인 작업 브랜치)

🎯 목표
- 각자 기능 브랜치를 만들어서
- dev를 기준으로 작업하고
- main/dev를 직접 건드리지 않는다

---

`2-1)` feature 브랜치 생성 규칙

브랜치 이름은 누가/무슨 기능이 보이게:

예시
- `feature/youjung-login`
- `feature/minsu-dashboard`
- `feature/jihye-ingest-api`

---

`2-2)` feature 브랜치는 dev에서 뽑기

> ⚠️ 실수 1등: main에서 feature를 뽑아버림
> → 나중에 dev와 히스토리가 꼬일 수 있음

시작전 체크:
```bash
git status   # 변경사항 없나 확인(working tree clean이면 OK)
git branch   # 지금 어디(main/dev/feature)인지 확인
```

dev로 이동 + 최신화 (항상 먼저)
```bash
git checkout dev
git pull origin dev
```

feature 브랜치 생성 + 이동
브랜치 이름 규칙: `feature/<이름>-<작업>`
예: `feature/youjung-login`
```bash
git checkout -b feature/youjung-login
```

생성 확인 (별`(*)` 위치 확인)
```bash
git branch
```

예상:
```
  main
  dev
* feature/youjung-login
```
✅ 별`(*)`이 feature에 있으면 개인 작업 브랜치로 이동 완료  
이제부터 코드는 여기서만 작업하고, 다음 단계(작업→commit→push→PR)는 이후 파트에서 진행.

---

### 3️⃣ feature에서 작업 → commit → push (실전)

✅ 이 단계의 의미
	이 단계는 개인이 맡은 기능을 구현하고, 그 결과를 팀 저장소(GitHub)에 공유하기 직전까지의 과정입니다.
아직 병합(merge)은 하지 않고, 내가 이런 작업을 했어요 라고 원격에 브랜치를 공개하는 단계

즉,
- ❌ main / dev는 건드리지 않고
- ✅ feature 브랜치에서만 작업
- ✅ 작업 결과를 커밋으로 남기고
- ✅ GitHub에 push 해서 PR 준비 상태를 만든다

---
`3-1)` 기능 작업하기 (파일 추가/수정)

예시: 로그인 기능을 맡았다고 가정  
→ 실제 로직이 아니라 형태만 있는 더미 코드로 충분
```bash
mkdir -p backend
echo "def login(username, password):" > backend/login.py
echo "    return 'login ok (dummy)'" >> backend/login.py
```
📌 포인트
- feature 브랜치에서는 자유롭게 파일 생성/수정 가능
- 아직 팀에 반영된 건 아님 (내 로컬 작업일 뿐)
    
상태 확인:
```bash
git status
```

예상:
```
Untracked files:
  backend/login.py
```
의미:
	새 파일이 생겼는데, Git이 아직 기록하지는 않았다

---
`3-2)` add → commit : 작업 결과를 하나의 기록으로 남기기
왜 commit을 하나?
- 이 시점까지의 작업은 의미 있는 단위다
- 나중에 리뷰 / 롤백 / 히스토리 추적이 가능해짐
```bash
git add backend/login.py
git commit -m"Add dummy login function"
```
커밋 메시지 규칙
- 무엇을 했는지 한 문장
- 기능 단위로 끊어서 커밋
- 예:
    - `Add dummy login function`
    - `Create dashboard skeleton`
    - `Add ingest API placeholder`
---
`3-3)` feature 브랜치를 원격에 push (공유 단계)
```bash
git push -u origin feature/youjung-login
```
이 명령의 의미
- `feature/youjung-login` 브랜치를 GitHub에 처음 등록
- 이후부터:
    - GitHub에서 브랜치 선택 가능
    - Pull Request 생성 가능
    - 팀원이 내 코드를 볼 수 있음
        
`-u` 옵션의 의미:
	앞으로는 `git push` 만 쳐도 이 브랜치가 `origin/feature/youjung-login`으로 자동 연결됨

3번단계를 요약하면:
- feature 브랜치에서 작업하고  
- commit으로 작업 단위를 남긴 뒤  
- push로 “내 작업 브랜치를 팀에 공개”하는 단계다.

---

### 4️⃣ PR(Pull Request)로 feature → dev 병합 (협업 핵심)

`4-1)` GitHub에서 PR 만들기
1. GitHub 리포 접속
2. Pull requests → New pull request
3. base: `dev` ← 합쳐질 곳(통합 브랜치)
4. compare: `feature/youjung-login` ← 내 작업 브랜치
5. Create pull request

✅ base/compare를 강조하는 이유
- 실수 1등: base를 `main`으로 걸어버림  
    → 배포 브랜치에 바로 들어가서 팀 사고남
![[Pasted image 20251219181920.png]]
---
PR 설명은 “리뷰가 가능하게” 3줄만
PR 템플릿
- 변경 내용: (무엇을 했는지)
- 테스트 방법: (어떻게 확인했는지)
- 영향 범위 / TODO: (나중에 할 일, 주의사항)
    

예)
- 변경: backend/login.py 더미 로그인 함수 추가
- 테스트: 파일 생성 및 함수 호출 확인
- TODO: JWT 연동 예정
![[Pasted image 20251219182251.png]]
---

`4-2)` 리뷰(선택) → 병합(Merge)

수업에서는 보통 “자기 PR을 자기 손으로 merge”까지 경험시키면 좋아요.

병합 옵션 중 추천: Squash and merge
- 장점
    - 여러 커밋이 1개로 정리됨(히스토리 깔끔)
    - 초보자가 커밋을 많이 쪼개도 결과는 깔끔하게 유지됨
        
- 언제 쓰나?
    - 기능 단위로 하나의 결과만 남기고 싶을 때(수업/협업에 매우 적합)

![[Pasted image 20251219182713.png]]

🔁 병합 전체 흐름 (지금 화면 위의 이미지 기준)

1️⃣ PR 상태 확인
- `Open` 상태 ✅
- `No conflicts with base branch` ✅  
    → 자동 병합 가능 상태

2️⃣ 초록색 버튼 클릭( 빨간 박스 친 부분)
이 버튼을 클릭하면 병합 방식 선택 화면이 나옵니다.

![[Pasted image 20251219182910.png]]
	PR 병합이 정상적으로 완료된 상태

==PR 상태가 `Merged` 로 표시됨
- 제목 왼쪽에 보라색 `Merged` 배지
- 문구:
	`handgonpo merged 1 commit into dev from feature/youjung-login`
    
👉 의미
- `feature/youjung-login` 브랜치의 변경 사항이
- `dev` 브랜치로 공식적으로 합쳐졌다는 뜻
    
---
==Pull request successfully merged and closed

화면 중간에 이 문구가 보입니다:
Pull request successfully merged and closed
	
👉 의미
- PR이 병합되었고
- 더 이상 열려 있는 요청(Open PR)이 아님
- 이 PR은 이제 과거 기록이 됨
    
---
==`Delete branch` 버튼이 나타남

`the feature/youjung-login branch can be safely deleted`
	
👉 의미
- 이 feature 브랜치는 할 일을 다 끝낸 상태
- 더 이상 유지할 필요가 없으므로 삭제해도 안전
- (수업에서는 삭제까지 경험시키는 게 좋음)
    
---
==하단 커밋 히스토리 표시

`handgonpo merged commit ecd13cd into dev now`
	
👉 의미
- 실제로 `dev` 브랜치에 커밋이 들어갔다
- 말이 아니라 Git 히스토리에 반영됨
	
---

### 5️⃣ dev 최신화(반드시!) → 다음 feature로 넘어가기

✅ 이 단계의 의미
	이 단계는 병합이 끝난 뒤, 다시 팀 기준선(dev)을 맞추는 작업입니다.
	
PR로 병합은 GitHub 서버에서만 일어났고, 내 로컬 dev 브랜치는 아직 예전 상태일 수 있습니다.
👉 그래서 다음 feature 작업을 시작하기 전에 반드시 로컬 dev를 최신 dev와 동기화해야 합니다.

🎯 왜 반드시 해야 하나?
- 다른 팀원의 기능이 dev에 합쳐졌는데
- 나는 옛날 dev 기준으로 계속 작업하면
- 다음 PR에서:
    - ❌ 불필요한 충돌 발생
    - ❌ “왜 이 파일이 여기서 바뀌었지?” 혼란
    - ❌ 리뷰 시 “기존 코드까지 같이 바뀜” 문제 발생
        
👉 충돌의 70%는 dev 최신화 안 해서 생깁니다.

---

실습: 병합 후 dev 최신화 (필수 루틴)
```bash
git checkout dev        # 1️⃣ 다시 dev 브랜치로 이동
git pull origin dev     # 2️⃣ GitHub의 최신 dev를 로컬 dev에 반영
```
 이 두 줄이 의미하는 것
- `git checkout dev`
    - 이제부터 팀 공용 기준선에서 작업하겠다는 뜻
        
- `git pull origin dev`
    - 방금 PR로 합쳐진 변경 사항들을 내 로컬에 반영

정상 상태 확인
```bash
git status
```

정상이라면:
```bash
On branch dev
Your branch is up to date with 'origin/dev'.

nothing to commit, working tree clean
```
👉 이 상태가 다음 feature를 뽑아도 안전한 상태입니다.

---

### 6️⃣ 최종 배포 단계: dev → main 병합

✅ 이 단계의 의미
- 지금까지는 feature → dev로 기능들을 모으는 과정
- 이제는 dev에 모인 기능들을 최종본(main)에 반영하는 과정
    
- 즉, main은 “발표/배포용 최종본”이라 아무 때나 막 합치면 안 되고  
    → Release PR로 한 번 더 검토하고 병합하는 게 원칙
    
> 한 줄로:  
> dev에서 검증된 것만 main으로 올린다.

---
`6-0) `병합 전 체크(필수)

① dev가 최신인지 확인
```bash
git checkout dev
git pull origin dev
```

② dev가 깨끗한 상태인지 확인(미커밋 없는지)
```bash
git status
```

정상이면:
```
nothing to commit, working tree clean
```

`6-1)` GitHub에서 Release PR 만들기 (dev → main)

✅ PR 생성 방법
1. GitHub 저장소 → **Pull requests** → **New pull request**
2. **base: main** (최종본)
3. **compare: dev** (통합본)
4. “Create pull request” 클릭

✅ PR 제목 추천(수업용)
- `Release: merge dev into main`
- `Release v1: integrate features`

 ✅ PR 설명(간단 템플릿)
- 포함 기능: login 더미 함수 포함, README 업데이트 포함
- 테스트: dev 기준으로 pull 후 파일 존재 확인
- 비고: 다음 스프린트에서 기능 확장 예정

`6-2)` Merge(병합) 실행

PR 화면에서 **Merge pull request** 버튼을 누릅니다.
- 실습에서는 보통 **Squash and merge** 또는 **Merge commit** 둘 다 가능
- 초보자 수업에서는 **Squash and merge** 추천
    → dev에서 여러 커밋이 있었어도 main엔 “릴리즈 1개 커밋”으로 깔끔히 남음
    
✅ 병합되면:
- GitHub의 `main` 브랜치가 최신 기능을 포함한 최종본이 됩니다.

`6-3)` 로컬 main도 최신화 (병합 후 반드시!)
	GitHub에서 main이 바뀌었으니, 내 컴퓨터(main)도 최신으로 맞춰야 합니다.
```bash
git checkout main
git pull origin main
```

✅ 확인 포인트
```bash
gitlog --oneline --max-count=5
```
- 방금 Release PR로 합쳐진 커밋이 보이면 정상

---
### 충돌(conflict) 실습 (의도적으로!)

🎯 목표
- 충돌이 왜 생기는지 직접 체험한다
- 충돌 표시(<<<<<<<)를 읽고 의미를 이해한다
- 파일을 열어 최종 버전을 스스로 결정한다
- 해결 후 `add → commit → push`까지 마무리한다

---

0️⃣ 시작 전 준비 (실습 성공률 100% 체크)

✅ (학생) 반드시 프로젝트 폴더로 이동
```bash
cd ~/git-day8-practice
```

✅ (학생) 브랜치가 main인지 확인
```bash
git branch
```

예상:
```
*main
```

✅ (학생) 작업 중 변경사항 없는지 확인
```bash
git status
```

정상(안전) 상태:
```
nothingtocommit, working tree clean
```

> ⚠️ 만약 modified가 있으면
> 이 충돌 실습이 꼬일 수 있으니 다시 clean 상태로 만든 뒤 진행하세요.

---

1️⃣ 충돌 상황 만들기

`1-1)` (강사) GitHub에서 README 수정 (원격 수정)

강사 행동(브라우저)
1. GitHub 저장소 열기
2. `README.md` 클릭
3. ✏️ Edit 버튼 클릭
4. 아래처럼 특정 줄을 정확히 수정하기 (충돌을 만들기 위한 같은 줄)

예시(README 일부):
```markdown
# git-day8-practice
이 줄은 원격에서 수정했습니다.
```

1. Commit changes 클릭 (main에 바로 반영)

✅ 이제 GitHub(main)에는 “원격 수정 커밋”이 생김

---

`1-2)` (학생) 로컬에서도 “같은 줄”을 다르게 수정 (로컬 수정)
학생 로컬에서 README 열기 (둘 중 하나 사용)

방법 A) nano 사용
```bash
nano README.md
```

방법 B) VSCode 사용
```bash
code README.md
```

그리고 강사가 수정한 그 줄(같은 줄)을 다르게 바꿉니다:

예:
```markdown
# git-day8-practice
이 줄은 로컬에서 수정했습니다.
```

저장 후 종료:
- nano: `Ctrl + O` → Enter → `Ctrl + X`
- VSCode: `Ctrl + S`

---

`1-3)` (학생) 로컬 변경 commit 만들기
```bash
git add README.md
git commit -m"Update README locally"
```

✅ 지금 상태 요약
- GitHub(main): “이 줄은 원격에서 수정했습니다.”
- 로컬(main): “이 줄은 로컬에서 수정했습니다.”
- 같은 파일 같은 줄을 서로 다르게 수정함 → 충돌 조건 완성

---

2️⃣ git pull 실행 → 충돌 발생

`2-1)` (학생) pull 실행
```bash
git pull
```

예상 출력(비슷한 형태):
```
Auto-merging README.md
CONFLICT (content): Mergeconflictin README.md
Automatic merge failed; fix conflictsandthencommit the result.
```

✅ 이 메시지가 나오면 “충돌 실습 성공”입니다.

---

3️⃣ 충돌 표시(<<<<<<<) 이해하기

`3-1)` 충돌 난 파일 열기
```bash
nano README.md
```

또는
```bash
code README.md
```

파일 안에 이런 표시가 생겨 있습니다:
```
<<<<<<< HEAD
이 줄은 로컬에서 수정했습니다.
=======
이 줄은 원격에서 수정했습니다.
>>>>>>> origin/main
```

### 이 표시의 의미(학생용)
- `<<<<<<< HEAD` ~ `=======`
    → 내 로컬 버전
    
- `=======` ~ `>>>>>>> origin/main`
    → GitHub(원격) 버전
    
> 즉, Git이 말하는 것: 둘 다 다르게 바뀌었는데, 내가 자동으로 선택 못하겠어. 너가 결정해줘.

---
4️⃣ 충돌 해결하기 (의사결정 단계)

`4-1)` 선택 방법 3가지 중 하나를 고르게 하기

✅ 선택 A: 로컬 버전만 남기기
```markdown
# git-day8-practice
이 줄은 로컬에서 수정했습니다.
```

✅ 선택 B: 원격 버전만 남기기
```markdown
# git-day8-practice
이 줄은 원격에서 수정했습니다.
```

✅ 선택 C: 둘을 합쳐서 새 문장으로 만들기(추천 ⭐)
```markdown
# git-day8-practice
이 줄은 로컬과 원격 수정을 합쳐 최종 버전으로 정리했습니다.
```

---

`4-2)` 가장 중요한 규칙(절대 실수 금지)

👉 아래 표시들은 **최종 파일에 남으면 안 됩니다. 반드시 삭제**
- `<<<<<<< HEAD`
- `=======`
- `>>>>>>> origin/main`

즉, 충돌 해결 후 파일은 평범한 문장만 있는 상태가 되어야 합니다.

---

5️⃣ 해결 후 add → commit → push

`5-1)` 충돌 해결됐는지 상태 확인
```bash
git status
```

예상:
```
both modified: README.md
```

또는
```
Unmerged paths:
README.md
```

👉 아직 해결 전이면 Unmerged가 보일 수 있음
👉 파일을 제대로 정리하고 저장했는지 다시 확인하세요.

---

`5-2)` 해결한 파일 스테이징
```bash
git add README.md
```

`5-3)` 충돌 해결 커밋 만들기
```bash
git commit -m"Resolve README merge conflict"
```

`5-4)` 원격에 push
```bash
git push
```

✅ GitHub에서 README를 열어보면 내가 결정한 최종 버전이 반영되어 있어야 합니다.

---

✅ 실습 체크포인트

Q1. “왜 충돌이 났나?”
- 같은 파일, 같은 줄을 **서로 다르게 수정**했기 때문

Q2. “충돌은 언제 자주 나나요?”
- README, 설정파일, 공용 파일(routers, urls, settings) 같은 곳에서 흔함

Q3. “충돌은 실패인가요?”
- 아니요. **의사결정 과정**입니다.

---
🧪 수업용 미션: “2명 팀으로 충돌 만들기”

- 학생 A: GitHub에서 README 수정(웹)
- 학생 B: 로컬에서 같은 줄 수정 후 commit
- 학생 B: pull 해서 충돌 해결
- 학생 A: PR 리뷰처럼 “최종 문장 더 명확히” 코멘트

---
✅ 한 줄 정리
> 충돌은 누가 틀렸다가 아니라 둘 다 바뀌었으니 최종 버전을 선택하라는 뜻이다.
---
# 팀 프로젝트 Git 구조 설계 & 협업 실습

> 💻 전제
- WSL(우분투) 환경
- Git 설치 완료
- GitHub 계정 보유
- HTTPS 방식으로 push
- **혼자 실습하지만 “팀 협업 상황”을 가정**

---

## 🧭 전체 흐름 한 줄 요약
> PM이 main → dev 기준선을 만들고 → 팀원은 dev 기준으로 feature 작업 → PR로 dev에 병합 → 최종적으로 dev → main 배포

---

### 0 준비 사항 (한 번만 하면 됨)

0-1. Git 사용자 정보 설정

> 이 정보는 모든 커밋에 기록되는 작성자 정보입니다.
```bash
git config --global user.name"홍길동"
git config --global user.email"youremail@example.com"
```

확인:
```bash
git config --global --list
```

---

0-2. Git 설치 확인
```bash
git --version
```

예시:
```bash
git version 2.43.0
```

---

### 1 로컬 새 프로젝트 폴더 만들기
> 목적: “아무 것도 없는 폴더 → Git이 관리하는 프로젝트” 만들기

```bash
cd ~
mkdir monitoring-platform-demo
cd monitoring-platform-demo
```

현재 위치 확인:
```bash
pwd
```

예시:
```bash
/home/youjung/monitoring-platform-demo
```

---

### 2 Git 초기화 + main 브랜치 첫 커밋

2-1. Git 저장소 초기화
```bash
git init
```

의미:
- 이 폴더부터 Git이 **변경 이력 관리 시작**

---

2-2. 최소한의 프로젝트 구조 만들기
```bash
mkdir backend dashboard docs
```

파일 생성:
```bash
echo"# Monitoring Platform Demo" > README.md
echo"print('Backend API placeholder')" > backend/app.py
echo"console.log('Dashboard placeholder');" > dashboard/app.js
echo"브랜치 전략과 Git 사용법을 문서화합니다." > docs/git-strategy.txt
```

확인:
```bash
ls
```

---

2-3. Git 상태 확인
```bash
git status
```

의미:
- 아직 Git이 추적하지 않는 파일(Untracked)

---

2-4. 첫 커밋 만들기
```bash
git add .
git commit -m"Initial project structure"
```

👉 **이 시점이 main 브랜치의 시작점**

---

### 3 GitHub 원격 저장소 생성 + 연결 + push

3-1. GitHub에서 새 저장소 생성

- 이름: `monitoring-platform-demo`
- ❌ README / .gitignore / License 체크하지 않기

---

3-2. 로컬 ↔ GitHub 연결
```bash
git remote add origin <https://github.com/><username>/monitoring-platform-demo.git
git branch -M main
git push -u origin main
```

확인:
```bash
git remote -v
```

---

### 4 dev 브랜치 만들기 (PM 역할 / 팀 기준선 세팅)

✅ 이 단계의 의미 (중요)
> ❗ 이 단계는 개인 기능 개발이 아님
> → **팀이 앞으로 협업할 기준선(dev)을 만드는 작업**
> → **원칙적으로 팀에서 1명(PM/리더)만 수행**

---

4-0. 현재 상태 점검
```bash
git branch
```

예상:
```
*main
```

---

4-1. main 기준으로 dev 생성
```bash
git checkout main# 기준 브랜치로 이동
git pull origin main# 원격 최신 상태 반영
git checkout -b dev# dev 브랜치 생성 + 이동
```

왜 중요한가?
- dev는 **팀 공용 기준선**
- 반드시 **최신 main 기준**이어야 히스토리가 꼬이지 않음

---

4-2. dev 브랜치를 원격에 올리기
```bash
git push -u origin dev
```

👉 이제 팀원 모두가 **dev 기준으로 feature를 뽑을 수 있음**

---

### 5 feature 브랜치 만들기 (개인 작업 브랜치)

🎯 목적
- 각자 기능을 **독립적으로** 개발
- main / dev는 직접 수정 ❌

---

5-1. feature 브랜치 생성 (반드시 dev 기준)
```bash
git checkout dev
git pull origin dev
git checkout -b feature/eunice-login
```

확인:
```bash
git branch
```

예상:
```
  main
  dev
* feature/eunice-login
```

---

### 6 feature에서 작업 → commit → push (개인 실전)

6-1. 기능 파일 추가
```bash
echo"def login(username, password):" > backend/login.py
echo"    return 'login ok (dummy)'" >> backend/login.py
```

확인:
```bash
git status
```

---

6-2. add → commit
```bash
git add backend/login.py
git commit -m"Add dummy login function"
```

---

6-3. feature 브랜치 원격에 push
```bash
git push -u origin feature/eunice-login
```

👉 GitHub에 **feature 브랜치 생성 완료**

---

### 7 PR(Pull Request) 생성: feature → dev

🎯 목적
- 코드를 바로 합치지 않고
- **검토 + 기록 + 협업 히스토리** 남기기

---

7-1. GitHub에서 PR 생성
- base: `dev`
- compare: `feature/eunice-login`

PR 설명 작성 위치
👉 **Create Pull Request 화면의 설명 입력창**

권장 템플릿:
```
변경 내용:
- 로그인 더미 함수 추가

테스트 방법:
- 파일 생성 확인

TODO:
- JWT 인증 연동 예정
```

---

7-2. Merge (병합)
- 버튼 위치: **PR 화면 하단**
- 추천 옵션: **Squash and merge**

왜 Squash?
- 여러 커밋 → 1개로 정리
- 히스토리 깔끔
- 교육/협업에 적합

👉 Merge 완료 시
- feature → dev 반영
- PR 상태: **Merged**

---

### 8 dev 최신화 (필수 습관)

🎯 왜 필요한가?
- 다른 사람이 합친 코드 반영
- 다음 feature 작업 전 기준선 정리

```bash
git checkout dev
git pull origin dev
```

👉 **다음 feature로 넘어가기 전 항상 수행**

---

### 9 (선택) 추가 feature 반복 실습
- `feature/ingest-api`
- `feature/dashboard`

👉 **모두 동일한 패턴**

1. dev 최신화
2. feature 생성
3. 작업 → commit → push
4. PR → dev 병합

---

### 10 최종 배포 단계: dev → main

🎯 이 단계의 의미
- 개발 완료
- **실제 발표 / 배포용 코드 확정**

---

10-1. GitHub에서 Release PR 생성
- base: `main`
- compare: `dev`

설명 예:
```
Release:
- auth / ingest / dashboard 기능 통합
```

---

10-2. Merge 후 로컬 main 최신화
```bash
git checkout main
git pull origin main
```

👉 이제 main = **최종 결과물**

---

✅ 이 실습으로 반드시 체감해야 하는 것
1. main은 안전하게 유지된다
2. dev는 팀 통합 기준선이다
3. feature는 개인 작업 공간이다
4. PR은 협업의 핵심 도구다
5. merge 후에는 반드시 pull 한다
6. Git은 “명령어”가 아니라 **협업 규칙**이다

