종류 예시:
- 하이브리드 검색 포털: 키워드(BM25) + 벡터 유사도 결합한 정확 검색   
- 멀티문서 요약 질의: 수십 문서에서 찾아 모아 한 번에 답변  
- 실시간 크롤링→인덱싱 파이프라인: 주기 수집→청크→임베딩→Chroma/PGVector 반영
---
##### 멀티문서 요약 질의(수십 문서를 찾아 모아 한 번에 답변)
내가 모아 둔 문서를 먼저 벡터로 인덱싱해 두고, 화면에서 질문을 치면 그 인덱스 기반으로 검색→요약해서 답을 보여주는 구조

`0)` 새 프로젝트 & 설치
```bash
mkdir multi-doc-qa && cd multi-doc-qa

# 가상환경(선택)
python -m venv .venv && source .venv/bin/activate   # (Windows: .venv\Scripts\activate)

# 필수 패키지 설치
pip install langchain==0.1.10 langchain-openai langchain-chroma chromadb pypdf python-dotenv fastapi uvicorn
```

`.env`
```env
OPENAI_API_KEY=sk-...              # OpenAI 키
OPENAI_MODEL=gpt-3.5-turbo-0125    # 기본 LLM (원하면 gpt-4o-mini)
EMBED_MODEL=text-embedding-3-small # 임베딩 모델(가성비)
CHROMA_DIR=./chroma                # 벡터DB 영구화 경로
RAG_COLLECTION=kb                  # 컬렉션명
```

`1)` 디렉토리 구조
```bash
multi-doc-qa/
├─ app/
│  ├─ main.py
│  ├─ api/
│  │   └─ search_routes.py
│  └─ services/
│      ├─ retriever.py
│      └─ summarize.py
├─ scripts/
│  └─ index_docs.py
├─ documents/               # ← 여기에 txt/pdf 문서들 넣기
├─ web/
│  └─ index.html            # 정적 프론트
├─ chroma/                  # (자동생성) Chroma 영구 저장소
└─ .env
```
`app/`, `app/api/`, `app/services/`에 `__init__.py`(빈 파일)를 만들어두면 import가 안정적입니다.

`2)` 문서 인덱싱(오프라인) — `scripts/index_docs.py`
- `documents/` 폴더의 TXT/PDF를 읽어 청크로 쪼개고,
- OpenAI 임베딩으로 벡터화하여 Chroma에 영구 저장합니다.
```python
# scripts/index_docs.py
import os, glob, hashlib
from dotenv import load_dotenv
from pypdf import PdfReader
import chromadb
from langchain_openai import OpenAIEmbeddings
from langchain_chroma import Chroma
from langchain.text_splitter import RecursiveCharacterTextSplitter

load_dotenv()
DOCS_DIR   = os.getenv("DOCS_DIR", "documents")
CHROMA_DIR = os.getenv("CHROMA_DIR", "chroma")
COLLECTION = os.getenv("RAG_COLLECTION", "kb")
EMBED_MODEL= os.getenv("EMBED_MODEL", "text-embedding-3-small")

def read_txt_pdf(path: str) -> str:
    if path.lower().endswith(".txt"):
        return open(path, "r", encoding="utf-8", errors="ignore").read()
    if path.lower().endswith(".pdf"):
        reader = PdfReader(path)
        pages = [page.extract_text() or "" for page in reader.pages]
        return "\n".join(pages)
    return ""

def sha1(s: str) -> str:
    return hashlib.sha1(s.encode("utf-8")).hexdigest()

def main():
    os.makedirs(DOCS_DIR, exist_ok=True)
    os.makedirs(CHROMA_DIR, exist_ok=True)

    files = sorted(glob.glob(os.path.join(DOCS_DIR, "*.*")))
    if not files:
        print(f"[warn] {DOCS_DIR}/ 에 문서를 넣고 다시 실행하세요.")
        return

    splitter = RecursiveCharacterTextSplitter(chunk_size=900, chunk_overlap=150)
    embeddings = OpenAIEmbeddings(model=EMBED_MODEL)

    # Chroma(Client) 초기화 및 컬렉션 준비
    client = chromadb.PersistentClient(path=CHROMA_DIR)
    try:
        col = client.get_collection(COLLECTION)
    except:
        col = client.create_collection(COLLECTION)

    add_ids, add_docs, add_metas = [], [], []

    for f in files:
        raw = read_txt_pdf(f)
        if not raw.strip():
            print(f"[skip empty] {os.path.basename(f)}")
            continue

        chunks = splitter.split_text(raw)
        for i, ck in enumerate(chunks):
            # 같은 파일이 재인덱싱되어도 중복 추가되지 않도록 ID 해시
            _id = sha1(f"{os.path.basename(f)}::{i}::{ck[:80]}")
            add_ids.append(_id)
            add_docs.append(ck)
            add_metas.append({"source": os.path.basename(f), "chunk": i})

    if not add_ids:
        print("추가할 내용이 없습니다.")
        return

    # 이미 들어간 ID는 제외 (upsert 느낌)
    existing = set(col.get(ids=add_ids).get("ids", []))
    todo = [(i,d,m) for i,d,m in zip(add_ids, add_docs, add_metas) if i not in existing]
    if not todo:
        print("Nothing new to add.")
        return

    ids, docs, metas = zip(*todo)
    print(f"Embedding {len(ids)} chunks...")
    vecs = embeddings.embed_documents(list(docs))  # 배치 임베딩

    print("Upserting into Chroma...")
    col.add(ids=list(ids), documents=list(docs), embeddings=list(vecs), metadatas=list(metas))
    print(f"Done. Indexed {len(ids)} chunks into '{COLLECTION}' at {CHROMA_DIR}")

if __name__ == "__main__":
    main()
```
인덱싱(벡터화) 실행
```bash
python scripts/index_docs.py
```
- `documents/` 안의 파일들을 청크 → 임베딩 → Chroma(`chroma/`)에 저장합니다.
- 완료 후 “Indexed N chunks …” 메시지가 뜹니다.

`3)` 검색+요약 서비스 — `app/services/retriever.py`
- Chroma를 영구 저장소에서 로드하고,
- 리트리버 핸들을 제공합니다.
```python
# app/services/retriever.py
import os
from dotenv import load_dotenv
from langchain_openai import OpenAIEmbeddings
from langchain_chroma import Chroma

load_dotenv()
CHROMA_DIR = os.getenv("CHROMA_DIR", "chroma")
COLLECTION = os.getenv("RAG_COLLECTION", "kb")
EMBED_MODEL= os.getenv("EMBED_MODEL", "text-embedding-3-small")

_embeddings = OpenAIEmbeddings(model=EMBED_MODEL)

# 이미 인덱싱된 컬렉션 로드
_vectordb = Chroma(
    collection_name=COLLECTION,
    embedding_function=_embeddings,
    persist_directory=CHROMA_DIR,
)

def get_retriever(k: int = 8):
    return _vectordb.as_retriever(search_kwargs={"k": k})
```

`4)` 멀티문서 요약 체인 — `app/services/summarize.py`
- 리트리버로 **여러 청크**를 가져온 뒤,
- Stuff 체인(문서 한 번에) 또는 Map-Reduce 체인(문서 많을 때)을 사용해  
    질문에 대한 최종 답변과 출처를 만듭니다.
```python
# app/services/summarize.py
import os
from typing import Dict, Any, List
from dotenv import load_dotenv
from langchain_openai import ChatOpenAI
from langchain_core.documents import Document
from langchain_core.prompts import ChatPromptTemplate
from langchain.chains.combine_documents import (
    create_stuff_documents_chain,
    create_map_reduce_documents_chain,
)
from .retriever import get_retriever

load_dotenv()
MODEL = os.getenv("OPENAI_MODEL", "gpt-3.5-turbo-0125")

# --- 공통 프롬프트 ---
# 컨텍스트 밖 추론을 금지하고, 출처(파일명)를 답변 하단에 나열하도록 유도
BASE_INSTRUCTIONS = """\
다음 사용자 질문에 대해, 제공된 문서 컨텍스트만 근거로 한국어로 답하세요.
컨텍스트에 없거나 불확실하면 "모르겠습니다"라고 답하세요.
답변 마지막에 참고한 출처(파일명)를 bullet로 나열하세요.
"""

STUFF_PROMPT = ChatPromptTemplate.from_messages([
    ("system", BASE_INSTRUCTIONS + "\n\n[컨텍스트 문서]\n{context}"),
    ("human", "[질문]\n{question}")
])

MAP_PROMPT = ChatPromptTemplate.from_messages([
    ("system", "아래 문서 청크를 바탕으로 질문에 대한 핵심 답변 포인트만 bullet로 정리하세요.\n문서가 질문과 무관하면 '무관'이라고만 적으세요."),
    ("human", "문서 청크:\n{context}\n\n질문: {question}")
])

REDUCE_PROMPT = ChatPromptTemplate.from_messages([
    ("system", BASE_INSTRUCTIONS + "\n아래는 여러 문서 청크로부터 뽑은 핵심 포인트 목록입니다. 중복/모순을 정리해 최종 답을 쓰세요."),
    ("human", "핵심 포인트들:\n{context}\n\n질문: {question}")
])

_llm = ChatOpenAI(model=MODEL, temperature=0.2)

def _collect_sources(docs: List[Document]) -> List[str]:
    # 중복 제거된 파일명 리스트
    seen, out = set(), []
    for d in docs:
        src = (d.metadata or {}).get("source")
        if src and src not in seen:
            seen.add(src)
            out.append(src)
    return out

def multi_doc_answer(question: str, k: int = 8) -> Dict[str, Any]:
    retriever = get_retriever(k=k)
    docs: List[Document] = retriever.get_relevant_documents(question)

    if not docs:
        return {"answer": "모르겠습니다. 관련 문서를 찾지 못했습니다.", "sources": [], "snippets": []}

    # 문서 양에 따라 Stuff(작음) vs Map-Reduce(큼) 선택
    total_chars = sum(len(d.page_content) for d in docs)
    if total_chars <= 8000:  # 대략적 기준
        chain = create_stuff_documents_chain(_llm, STUFF_PROMPT)
        answer = chain.invoke({"question": question, "context": docs})
    else:
        map_chain = create_stuff_documents_chain(_llm, MAP_PROMPT)
        reduce_chain = create_stuff_documents_chain(_llm, REDUCE_PROMPT)
        chain = create_map_reduce_documents_chain(
            llm=_llm,
            map_chain=map_chain,
            reduce_chain=reduce_chain,
            token_max=8000,  # 필요 시 조정
        )
        answer = chain.invoke({"question": question, "context": docs})

    sources = _collect_sources(docs)
    snippets = [{
        "source": (d.metadata or {}).get("source"),
        "chunk": (d.metadata or {}).get("chunk"),
        "text": d.page_content[:220] + ("…" if len(d.page_content) > 220 else "")
    } for d in docs]

    return {"answer": answer, "sources": sources, "snippets": snippets}
```

`5)` API 라우터 — `app/api/search_routes.py`
```python
# app/api/search_routes.py
from fastapi import APIRouter, HTTPException
from pydantic import BaseModel, Field
from app.services.summarize import multi_doc_answer

router = APIRouter(prefix="/search", tags=["search"])

class QueryReq(BaseModel):
    query: str = Field(..., examples=["환불/반품 정책 요약해줘"])
    k: int = 8

@router.post("/query")
def query(req: QueryReq):
    q = (req.query or "").strip()
    if not q:
        raise HTTPException(400, "query is empty")
    result = multi_doc_answer(q, k=req.k)
    return result
```

`6)` FastAPI 앱 — `app/main.py`
```python
# app/main.py
from fastapi import FastAPI
from fastapi.middleware.cors import CORSMiddleware
from fastapi.staticfiles import StaticFiles
from fastapi.responses import RedirectResponse
from app.api.search_routes import router as search_router

app = FastAPI(title="Multi-Doc QA", version="1.0.0")

app.add_middleware(
    CORSMiddleware,
    allow_origins=["*"], allow_credentials=True,
    allow_methods=["*"], allow_headers=["*"],
)

# API 라우터
app.include_router(search_router)

# 정적 프론트 서빙
app.mount("/web", StaticFiles(directory="web", html=True), name="web")

@app.get("/")
def root():
    return RedirectResponse(url="/web/")
```

`7)` 정적 프론트 — `web/index.html`
- 검색창에 질문을 입력하면 `/search/query`로 POST
- 답변과 함께 출처 및 스니펫(가져온 문서 조각) 표시
```html
<!doctype html>
<html lang="ko">
<head>
  <meta charset="utf-8"/>
  <meta name="viewport" content="width=device-width,initial-scale=1"/>
  <title>멀티문서 요약 질의</title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-slate-50 text-slate-900">
  <div class="max-w-4xl mx-auto p-6">
    <h1 class="text-2xl font-bold mb-2">멀티문서 요약 질의 (LangChain)</h1>
    <p class="text-sm text-slate-600 mb-6">여러 문서에서 찾아 모아 한 번에 답변합니다.</p>

    <section class="bg-white rounded-2xl shadow p-4 mb-4">
      <form id="qform" class="flex gap-2 items-center">
        <input id="q" class="flex-1 border rounded-xl px-4 py-3 outline-none focus:ring w-full"
               placeholder="질문을 입력하세요 (예: 반품/환불 정책 핵심 요약)" />
        <input id="k" type="number" min="3" max="20" value="8"
               class="w-24 border rounded-xl px-3 py-3 text-sm" title="Top-k"/>
        <button id="go" class="px-5 py-3 rounded-xl bg-blue-600 text-white hover:bg-blue-700">
          질의
        </button>
      </form>
    </section>

    <section id="answer" class="bg-white rounded-2xl shadow p-5 mb-4 hidden">
      <h2 class="text-lg font-semibold mb-2">답변</h2>
      <div id="answerText" class="prose max-w-none whitespace-pre-wrap"></div>
      <div id="sources" class="mt-4"></div>
    </section>

    <section id="snippets" class="bg-white rounded-2xl shadow p-5 hidden">
      <div class="flex items-center justify-between mb-2">
        <h2 class="text-lg font-semibold">참고 스니펫</h2>
        <button id="toggleSnip" class="text-sm text-blue-600 hover:underline">펼치기/접기</button>
      </div>
      <div id="snipList" class="space-y-3"></div>
    </section>
  </div>

  <script>
    const form = document.getElementById('qform');
    const input = document.getElementById('q');
    const k = document.getElementById('k');
    const answerBox = document.getElementById('answer');
    const answerText = document.getElementById('answerText');
    const sourcesDiv = document.getElementById('sources');
    const snippetsBox = document.getElementById('snippets');
    const snipList = document.getElementById('snipList');
    const toggleSnip = document.getElementById('toggleSnip');

    let snipOpen = true;
    toggleSnip?.addEventListener('click', () => {
      snipOpen = !snipOpen;
      snipList.style.display = snipOpen ? 'block' : 'none';
    });

    form.addEventListener('submit', async (e) => {
      e.preventDefault();
      const q = input.value.trim();
      if (!q) return;
      setBusy(true);
      showLoading("질의 중…");

      try {
        const res = await fetch('/search/query', {
          method: 'POST',
          headers: {'Content-Type':'application/json'},
          body: JSON.stringify({ query: q, k: Number(k.value || 8) })
        });
        if (!res.ok) {
          const t = await res.text();
          showError(`오류(${res.status}): ${t}`);
          return;
        }
        const data = await res.json();
        renderResult(data);
      } catch (err) {
        showError('요청 실패: ' + err);
      } finally {
        setBusy(false);
      }
    });

    function renderResult(data) {
      answerBox.classList.remove('hidden');
      answerText.textContent = data?.answer || '(빈 응답)';
      sourcesDiv.innerHTML = "";
      if (Array.isArray(data?.sources) && data.sources.length) {
        const ul = document.createElement('ul');
        ul.className = 'list-disc pl-6 text-sm text-slate-700';
        data.sources.forEach(s => {
          const li = document.createElement('li');
          li.textContent = s;
          ul.appendChild(li);
        });
        sourcesDiv.innerHTML = `<h3 class="font-medium mb-1">참고 출처</h3>`;
        sourcesDiv.appendChild(ul);
      }

      if (Array.isArray(data?.snippets) && data.snippets.length) {
        snippetsBox.classList.remove('hidden');
        snipList.innerHTML = "";
        data.snippets.forEach(sn => {
          const div = document.createElement('div');
          div.className = 'border rounded-xl p-3';
          div.innerHTML = `
            <div class="text-sm text-slate-500 mb-1">📄 ${escapeHtml(sn.source)} (chunk ${sn.chunk})</div>
            <div class="text-sm whitespace-pre-wrap">${escapeHtml(sn.text)}</div>
          `;
          snipList.appendChild(div);
        });
      } else {
        snippetsBox.classList.add('hidden');
      }
    }

    function showLoading(msg) {
      answerBox.classList.remove('hidden');
      answerText.textContent = msg;
      sourcesDiv.innerHTML = "";
      snippetsBox.classList.add('hidden');
    }

    function showError(msg) {
      answerBox.classList.remove('hidden');
      answerText.textContent = msg;
      sourcesDiv.innerHTML = "";
      snippetsBox.classList.add('hidden');
    }

    function setBusy(b) {
      document.getElementById('go').disabled = b;
      input.disabled = b;
      k.disabled = b;
    }

    function escapeHtml(s) {
      return String(s).replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;');
    }
  </script>
</body>
</html>
```

`8)` 실행 & 테스트
문서 넣기  
`documents/` 폴더에 TXT/PDF 여러 개 복사

인덱싱
```bash
python scripts/index_docs.py
```

서버 실행
```bash
uvicorn app.main:app --reload --port 8000
```

설계 요점
- 인덱싱은 오프라인: 문서 변경 시에만 실행 → 비용/지연 절감
- Chroma 영구화: `CHROMA_DIR`로 재시작 시 재인덱싱 불필요
- 리트리버 k 조절: 너무 크면 잡음↑, 너무 작으면 누락↑ (8~12 권장)
- 스텝 선택: 문서 총 길이가 작으면 Stuff, 크면 Map-Reduce로 자동 선택
- 출처·스니펫 제공: 투명성/검증 가능성 향상
- 프론트 정적 서빙: 같은 서버에서 `/web/`로 UI 제공, CORS 최소화

